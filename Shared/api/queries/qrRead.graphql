query read($id: ID!) {
  qrRecord(id: $id) {
    id
    tea {
      id
      name
      type
      description
      tags {
        id
        color
        category {
          name
        }
        name
      }
    }
    bowlingTemp
    expirationDate
  }
}
